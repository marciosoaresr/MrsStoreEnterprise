@using MSE.WebApp.MVC.Extensions
@inject IUser AppUser

<ul class="navbar-nav">

    @if (AppUser.EstaAutenticado())
    {
    <div class="dropdown d-inline-block">
        <small class="title text-muted">Olá @AppUser.ObterUserEmail()</small>
        <a class="btn btn-primary btn-sm" asp-controller="Identidade" asp-action="Logout"> Sair</a>
        <!--<a href="#" class="icontext dropdown-toggle" data-toggle="dropdown" aria-expanded="false">-->
        @*<img class="icon icon-sm rounded-circle" src="https://s.gravatar.com/avatar/@this.HasEmailForGravatar(AppUser.ObterUserEmail())?d=mm&s=45">*@
        <!--</a>--> <!-- iconbox // -->
        <!--<div class="dropdown-menu dropdown-menu-left" x-placement="bottom-end" style="position: absolute; will-change: transform; top: 12px; left: 91px; transform: translate3d(150px, 32px, 0px);">
        <a class="dropdown-item" href="#">Meu Perfil</a>
        <a class="dropdown-item" href="#">Meus Pedidos</a>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" asp-controller="Identidade" asp-action="Logout">Sair</a>
    </div>-->
    </div>
    }
    else
    {
        <div class="widget-header">
            <small class="title text-muted">Bem vindo!</small>
            <div>
                <a class="btn btn-success btn-sm" asp-controller="Identidade" asp-action="Registro">Crie sua conta</a>
                <a class="btn btn-primary btn-sm" asp-controller="Identidade" asp-action="Login"> Login</a>
            </div>
        </div>
    }

</ul>
